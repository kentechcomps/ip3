apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: yolobackendcontainer
          image: kencehz/yolobackend:v1.0.0
          command: ["npm", "start"] 
          ports:
            - containerPort: 5000
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: yolobackendservice
spec:
  type: LoadBalancer
  selector:
    app: yolobackend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30100
  